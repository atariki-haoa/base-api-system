const path = require('path');

module.exports = {
  config: path.resolve('.', 'sequelize.js'), // Ruta al archivo de configuración de tu base de datos
  'models-path': path.resolve('./src/models'),       // Ruta al directorio donde se encuentran tus modelos
  'seeders-path': path.resolve('./seeders'),     // Ruta al directorio donde se encuentran tus seeders (es opcional)
  'migrations-path': path.resolve('./migrations')// Ruta al directorio donde se encuentran tus migraciones
};